/**
 * Temporary access rules for the public Alpha.
 * Keep these overrides centralized so the production economy and progression
 * targets can be restored without hunting through individual Core screens.
 */
export const PLATFORM_ALPHA_POLICY = {
  dailyCoinAllowance: 500,
  allCoverVariantsUnlocked: true,
} as const
