# MiniFugg Core brand assets

Core branding must follow `docs/BRAND_ASSETS.md`.

Canonical wordmark visual reference:

`/assets/imported/platform/logo/minifugg-logo-canonical-2026-09-06.png`

Do not add alternate local MiniFugg wordmarks here. Do not recreate the logo with CSS, text, a substitute font, or a new image-generation pass.

The canonical PNG remains the visual source of truth until the user explicitly validates a vector master. When that happens, keep one centralized Core primitive/path derived from that master and update `docs/BRAND_ASSETS.md` before replacing runtime references.
